name: flutter_agent_pupau_example
description: Example app to test Pupau Agent API
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_agent_pupau:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

